# Tasks: `jobs-reopen`

Status: tasks (SDD phase). Grounded by `proposal.md`, the delta spec `specs/jobs/spec.md`, and `design.md`. Design decisions **D1–D8 are pinned and MUST NOT be re-opened**; every task references the decision IDs it implements so apply can trace. Scenario coverage maps every delta scenario (S1–S29, design §7 checklist) to at least one task — see the matrix at the bottom.

Apply notes (non-negotiable):
- Strict TDD (`strict_tdd: true`): RED tasks precede their GREEN counterpart. A RED task is satisfied when the focused test fails for the intended reason (missing branch, wrong transition result, or a compile break on the sqlc shape are all accepted REDs — design §6).
- Checkboxes are marked `[x]` **only after the corresponding commit lands**, never at drafting time.
- `backend/internal/db/jobs.sql.go` and `backend/internal/db/querier.go` are **generated by sqlc — never hand-edited**. Regen via `cd backend && go tool sqlc generate` (Makefile target `make sqlc`).
- **Commit grouping (work-unit commits — tests land with the behavior they verify):**
  - **Commit A** — `feat(jobs): allow re-opening closed jobs via explicit status transition` → tasks 1.1–1.6 (`updateJob_test.go` + `updateJob.go`; D4/D5). Self-contained: the tree compiles and is green after this commit alone.
  - **Commit B** — `feat(jobs): gate PATCH updates on company activity via atomic UpdateJob guard` → tasks 2.1–2.3 (`jobs.sql` + sqlc regen + `jobRepository.go` + `updateJobRepository_test.go`; D1/D2/D7). Tasks 2.2 and 2.3 MUST land in this same commit (the adapter is compile-broken between regen and the `Update` rewrite — no full-suite gate between them).
  - **Commit C** — `test(jobs): integration coverage for re-open and the active-company update gate` → tasks 3.1–3.4 (`jobRepository_write_integration_test.go`; deferred RED for the D1 SQL, exactly as `jobs-create` Phase 7 did).
  - **Commit D (optional)** — comment-only sentinel doc refresh → task 5.1.
  - These three (or four) commits are also the chained-PR slices if the parent flips delivery from single-pr (forecast below).
- Locked scope (D8): exactly 4 authored files + 2 generated files change. `cmd/api/main.go`, DTOs, port, domain sentinels, handler, migrations, and the read side are untouched. Rollback = revert the merge commit; reverting `updateJob.go` alone restores closed terminality, reverting `jobs.sql` + `jobRepository.go` restores the pre-gate `:execrows` UpdateJob (regen reverted in lockstep).

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~815–1,080 authored additions+deletions across 6 files (excl. ~150–200 regenerated sqlc in `internal/db/jobs.sql.go` + ~2–4 in `querier.go`, which are excluded from the authored count but included in snapshot identity) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (Phase 1: use-case re-open — Commit A) → PR 2 (Phase 2: SQL guard + adapter + sqlc — Commit B) → PR 3 (Phase 3: integration tests — Commit C) |
| Delivery strategy | single-pr (pinned by preflight — requires an accepted size-exception; do not auto-chain) |
| Chain strategy | size-exception |

```text
Decision needed before apply: Yes
Chained PRs recommended: Yes
Chain strategy: size-exception
400-line budget risk: High
```

Rationale (per-file authored estimate, using design §5 exact shapes): `updateJob.go` ~30–40 (D4 case + D5 bypass + comment refresh); `db/queries/jobs.sql` ~55–75 (`:execrows`→`:one`, `active` + `upd` CTEs, `AND EXISTS`, scalar SELECT, guard doc-comment — re-indentation of the wrapped UPDATE counts); `jobRepository.go` ~40–55 (rewritten `Update` + `mapUpdateError` ErrNoRows branch + doc comments); `updateJob_test.go` ~310–410 (full-table flip, 9 new EditJob tests, narrowing of the two stale closed-terminal tests); `updateJobRepository_test.go` ~50–70 (2 new `mapUpdateError` cases); `jobRepository_write_integration_test.go` ~330–430 (8 new integration tests). Sum ≈ 815–1,080 — 2–2.7× the 400-line budget. Forecast rules require recommending chained PRs at High risk; because the preflight pinned `single-pr`, the apply agent MUST stop and record an accepted **size-exception** (or the parent must flip to chained PRs) before the first commit lands. Tasks are already split into autonomous work units with start/finish/verify/rollback boundaries so either path proceeds. No auto-chaining: this artifact only flags; the parent decides.

---

## Phase 1 — Use case: closed re-open transition logic (RED → GREEN; D4 + D5)

Files: `backend/internal/features/jobs/application/usecases/updateJob_test.go` (MOD), `backend/internal/features/jobs/application/usecases/updateJob.go` (MOD).
Commit group: **Commit A** (tasks 1.1–1.6 land together as one work-unit commit).

### 1.1 RED — Flip the transition matrix and narrow the stale closed-terminal tests (D4)
- [x] 1.1 RED — Extend `TestIsTransitionAllowed_FullTable`; narrow the two obsolete closed-terminal tests. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/application/usecases/updateJob_test.go` (MOD): extend `TestIsTransitionAllowed_FullTable` with the three `Closed` rows per D4 — `Closed→Draft = true`, `Closed→Published = true`, `Closed→Closed = false`; all six draft/published rows unchanged; refresh the helper's doc comment ("closed is terminal" is stale). In the same change, audit and narrow the two tests pinning the OLD closed-terminality: `TestEditJob_ClosedTerminalAnyBodyRejects` and `TestEditJob_ClosedTerminalStatusAnyRejected` — under the delta spec the only still-rejected cases are field-only (no `status`) and `status="closed"` on a closed row; rewrite them to assert exactly those (they will otherwise go red and contradict S4/S8/S9). `TestEditJob_UnknownStatusDefaultsFor` must keep passing — `default: return false` stays (D4).

- Verify: `cd backend && go test ./internal/features/jobs/application/usecases/ -run 'TestIsTransitionAllowed_FullTable|TestEditJob_ClosedTerminal|TestEditJob_UnknownStatusDefaultsFor'` → **RED** (the three Closed rows fail against the current `default: return false`; narrowed closed-terminal tests still pass). Rollback: revert the test hunk only.

### 1.2 RED — Closed re-open happy paths (S1, S2, S24, S25)
- [x] 1.2 RED — Add `TestEditJob_ClosedToDraftReopens` and `TestEditJob_ClosedToPublishedReopens`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/application/usecases/updateJob_test.go` (MOD): stub `GetForUpdate` returns a row with `JobStatus=Closed` and matching `UpdatedAt`; body `status="draft"` → assert `(view, nil)` with `view.Status=="draft"`, `repo.Update` called with `patch.Status==Draft` (capture via `writeStubRepo`); body `status="published"` → assert `view.Status=="published"` and `patch.Status==Published`. Assert the success path re-reads (authoritative view) exactly like the existing `TestEditJob_SuccessReReadsAuthoritativeView`.

- Verify: `cd backend && go test ./internal/features/jobs/application/usecases/ -run 'TestEditJob_ClosedTo(Draft|Published)Reopens'` → **RED** (the closed-terminal early return in `EditJob` rejects before `Update`). Rollback: revert the test hunk.

### 1.3 RED — Closed no-op and field-only rejections (S4, S8, S9)
- [x] 1.3 RED — Add `TestEditJob_ClosedToClosedRejects`, `TestEditJob_ClosedFieldOnlyRejects`, `TestEditJob_ClosedStatusAbsentLeavesClosed`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/application/usecases/updateJob_test.go` (MOD): `status="closed"` on a closed row → `(nil, ErrInvalidStatusTransition)`, `Update` NOT called; field-only body with NO `status` → `(nil, ErrInvalidStatusTransition)`, `Update` NOT called (S8); same field-only body → row `status` remains `"closed"` (S9 — assert via the stub's captured state / that no update reached the repo, mirroring the existing closed-terminal field-only pin).

- Verify: `cd backend && go test ./internal/features/jobs/application/usecases/ -run 'TestEditJob_Closed(ToClosed|FieldOnly|StatusAbsent)'` → **RED** (currently the early return rejects them — the first assertion on `ErrInvalidStatusTransition` may pass but the `Update` NOT-called pin is what must be verified as the intended RED where applicable; confirm each new test's specific assertion fails for the intended reason). Rollback: revert the test hunk.

### 1.4 RED — Atomic re-open + field mix (S6, S7, S19)
- [x] 1.4 RED — Add `TestEditJob_ClosedToDraftWithTitleApplies` and `TestEditJob_ClosedToPublishedWithDescriptionApplies`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/application/usecases/updateJob_test.go` (MOD): closed row + `{"status":"draft","title":"New"}` → assert `patch.Status==Draft` AND `patch.Title==&"New"` in the same captured `Update` call (S6); closed row + `{"status":"published","description":"new body"}` → `patch.Status==Published` AND `patch.Description==&"new body"` (S7). Assert `Update` is called exactly once (the transition + fields apply in one statement — no second call).

- Verify: `cd backend && go test ./internal/features/jobs/application/usecases/ -run 'TestEditJob_ClosedTo(DraftWithTitle|PublishedWithDescription)'` → **RED** (early return rejects before `Update`). Rollback: revert the test hunk.

### 1.5 RED — Re-open CAS and gate propagation (S14, S28, S29)
- [x] 1.5 RED — Add `TestEditJob_ClosedReopenStaleCASReturnsConflict` and `TestEditJob_UpdateErrCompanyNotActivePropagates`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/application/usecases/updateJob_test.go` (MOD): closed row + `status="draft"` + stale `If-Unmodified-Since` → `(latestEditorView, ErrConcurrencyConflict)`, `Update` NOT called (S14 — same shape as `TestEditJob_CASMismatchReturnsConflict`, closed variant). Stub `Update` returns `entities.ErrCompanyNotActive` → use case returns `(nil, ErrCompanyNotActive)` and does **NOT** re-read (`getForUpdateCalls` stays 1) — the gate error propagates untouched, no `ErrJobNotFound` special-case fires (S29).

- Verify: `cd backend && go test ./internal/features/jobs/application/usecases/ -run 'TestEditJob_(ClosedReopenStaleCASReturnsConflict|UpdateErrCompanyNotActivePropagates)'` → **RED** (stale-CAS test currently rejects with the terminal 400 before the CAS re-check; the gate test fails because `writeStubRepo` has no `updateErr` path wired — extend the stub with a programmable `updateErr error` if needed, in the test file). Rollback: revert the test hunk.

### 1.6 GREEN — Apply D4 + D5 in `updateJob.go`
- [x] 1.6 GREEN — Add the explicit `Closed` case and the closed-terminal bypass. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/application/usecases/updateJob.go` (MOD): (a) `isTransitionAllowed` gains `case valueobjects.Closed: return to == valueobjects.Draft || to == valueobjects.Published` and `default: return false` is **kept** (D4 — unknown statuses stay rejected); refresh the stale "closed is terminal" doc comment on the helper in the same commit (design §3 cosmetic note). (b) Replace the unconditional `if current.JobStatus == valueobjects.Closed { return nil, entities.ErrInvalidStatusTransition }` with the D5 guarded bypass: `reopens := newStatus != nil && (*newStatus == valueobjects.Draft || *newStatus == valueobjects.Published)`; reject only when `!reopens`; then the existing `isTransitionAllowed` check runs unchanged. Do NOT touch CAS, VO parse, salary validation, `repo.Update`, re-read, or `toEditorView` (design §5.1 — everything else byte-identical).

- Verify: tasks 1.1–1.5 tests pass; `cd backend && go test ./internal/features/jobs/application/usecases/` green — `TestEditJob_UnknownStatusDefaultsFor` and ALL pre-existing draft/published tests stay green (regression net proving D4/D5 relax nothing else); `cd backend && go build ./...`. Commit **A** lands here. Rollback: revert Commit A (or `updateJob.go` alone) — closed terminality restored.

### 1.7 REFACTOR (optional, tracked) — Sweep the use-case surface
- [ ] 1.7 REFACTOR — Confirm no behavioral leakage outside D4/D5. <!-- sdd-owner: implementation -->

Re-read `updateJob.go` (MOD): `default: return false` still rejects unknown from-statuses; the bypass applies ONLY when `newStatus != nil` and `*newStatus` is Draft/Published (field-only and `closed→closed` still 400); `ErrCompanyNotActive` propagation relies on the existing fall-through (no new special-case). No change to `jobService.go`, DTOs, or the port.

- Verify: `cd backend && go test ./internal/features/jobs/...` green; `cd backend && go vet ./internal/features/jobs/...` clean.

---

## Phase 2 — Adapter + sqlc: atomic active-company guard (RED → GREEN; D1/D2/D7)

Files: `backend/internal/features/jobs/infrastructure/postgres/updateJobRepository_test.go` (MOD — home of the existing `mapUpdateError` tests), `backend/db/queries/jobs.sql` (MOD), `backend/internal/db/jobs.sql.go` + `backend/internal/db/querier.go` (generated regen), `backend/internal/features/jobs/infrastructure/postgres/jobRepository.go` (MOD).
Commit group: **Commit B** (tasks 2.1–2.3 land together — 2.2 and 2.3 MUST be the same commit; the package is compile-broken between regen and the `Update` rewrite).

### 2.1 RED — `mapUpdateError` gains the `pgx.ErrNoRows` branch (D2)
- [x] 2.1 RED — Add the two `ErrNoRows` mapping tests. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/infrastructure/postgres/updateJobRepository_test.go` (MOD): add table-driven cases to the existing `mapUpdateError` suite — bare `pgx.ErrNoRows` → `entities.ErrCompanyNotActive`; wrapped `fmt.Errorf("...: %w", pgx.ErrNoRows)` → `entities.ErrCompanyNotActive` (errors.Is path). Existing cases (nil → nil; `23514` direct + wrapped → `ErrInvalidStatusTransition`; unknown `PgError` → pass-through; non-pg error → pass-through) stay untouched and green.

- Verify: `cd backend && go test ./internal/features/jobs/infrastructure/postgres/ -run MapUpdateError` → **RED** (new cases fail: no `ErrNoRows` branch yet). Rollback: revert the test hunk.

### 2.2 RED (compile) — Author `UpdateJob :one` and regenerate (D1, D7)
- [x] 2.2 RED — Author the D1 CTE guard; run sqlc; watch the adapter compile break. <!-- sdd-owner: implementation -->

`backend/db/queries/jobs.sql` (MOD): transform `UpdateJob` exactly per design §5.2/D1 — (a) `:execrows` → `:one`; (b) prepend the `active` CTE (`SELECT id FROM companies WHERE id = sqlc.arg('company_id')::uuid AND status = 'active'`); (c) wrap the existing UPDATE (SET list and `published_at` CASE **byte-for-byte unchanged**) in a `upd` CTE with `RETURNING id`; (d) append `AND EXISTS (SELECT 1 FROM active)` to the WHERE (row-narrowing predicates `id`, `company_id`, `deleted_at IS NULL`, `updated_at = cas_token` unchanged — D3); (e) replace the terminator with the scalar `SELECT EXISTS(SELECT 1 FROM active) AS guard_passed, (SELECT count(*) FROM upd) AS updated_count;` (always exactly one row — `pgx.ErrNoRows` unreachable on the designed path). No new `sqlc.arg` — `UpdateJobParams` stays unchanged. Refresh the query doc comment to describe the guard/observability shape. `CreateJob`, `SearchJobs`, `GetJobByID`, `GetJobForUpdate` stay byte-identical.

Then run `cd backend && go tool sqlc generate` (Makefile alias `make sqlc`): `backend/internal/db/jobs.sql.go` gains `UpdateJobRow {GuardPassed bool; UpdatedCount int64}` (field names pinned by the SQL aliases) and `UpdateJob(ctx, …) (UpdateJobRow, error)`; `backend/internal/db/querier.go`'s `Querier` interface signature changes to match. Confirm `UpdateJobParams` is unchanged and the other four queries are regenerated verbatim.

- Verify: `cd backend && go build ./...` → **RED (compile)** — `jobRepository.go:143`'s `rows, err := r.queries.UpdateJob(...)` then `rows == 0` no longer compiles against `(UpdateJobRow, error)`. The compile break is the accepted RED (design §6 item 8). Do NOT fix the adapter yet — 2.3 is the same commit. Rollback: revert the `jobs.sql` hunk + regen together.

### 2.3 GREEN — Rewrite `Update` + `mapUpdateError` (D2/D3)
- [x] 2.3 GREEN — Implement the guard-outcome inspection and the ErrNoRows defense branch. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/infrastructure/postgres/jobRepository.go` (MOD): `Update` becomes — `row, err := r.queries.UpdateJob(ctx, buildUpdateJobParams(id, companyID, patch, casUpdatedAt))`; `err != nil` → `mapUpdateError(err)`; `!row.GuardPassed` → `entities.ErrCompanyNotActive` (designed path, D2); `row.UpdatedCount == 0` → `entities.ErrJobNotFound` (CAS lost / cross-company / soft-delete race with active company — D3); else `nil`. Refresh the `Update` doc comment (guard semantics + result matrix). `mapUpdateError` gains, BEFORE the `errors.As` block: `if errors.Is(err, pgx.ErrNoRows) { return entities.ErrCompanyNotActive }` documented as defense-in-depth (mirrors `mapCreateError`; unreachable via the designed flow — design §5.3). The `23514 → ErrInvalidStatusTransition` branch and pass-through are unchanged. `buildUpdateJobParams`, `toJobForUpdateEntity`, and the `var _ repositories.JobRepository = (*JobRepository)(nil)` assertion are untouched.

- Verify: 2.1 tests pass; `cd backend && go test ./internal/features/jobs/...` green end-to-end (tree compiles again); `cd backend && go build ./...`. Commit **B** lands here (2.1–2.3 atomically). Rollback: revert Commit B (with Commit A reverted if mid-sequence) — `jobs.sql` + `jobRepository.go` + the two generated files revert in lockstep.

### 2.4 REFACTOR (optional, tracked) — Adapter hygiene
- [ ] 2.4 REFACTOR — Confirm generated-not-hand-edited and no duplicated helpers. <!-- sdd-owner: implementation -->

`git diff --stat backend/internal/db/` to confirm `jobs.sql.go`/`querier.go` changes are exactly the regen (no hand-edit); confirm `mapUpdateError`'s new branch does not duplicate `mapCreateError` logic beyond the shared `errors.Is` idiom; `Update`'s guard inspection has no dead branches per the D1 result matrix.

- Verify: `cd backend && go test ./internal/features/jobs/...` still green; `go tool sqlc generate` re-run is idempotent (`git diff` empty after a second run).

---

## Phase 3 — SQL-level integration: re-open + active-company gate (RED/GREEN; deferred RED for the D1 SQL)

File: `backend/internal/features/jobs/infrastructure/postgres/jobRepository_write_integration_test.go` (MOD, `//go:build integration`).
Commit group: **Commit C** (tasks 3.1–3.4 land together — one work-unit commit written against the landed SQL; RED and GREEN coincide for integration tests, exactly as `jobs-create` Phase 7).

### 3.1 RED/GREEN — Re-open transitions at the SQL level (S1, S2, S3)
- [x] 3.1 RED/GREEN — Add `TestUpdate_ClosedToDraftReopens` and `TestUpdate_ClosedToPublishedPreservesPublishedAt`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/infrastructure/postgres/jobRepository_write_integration_test.go` (MOD): fixture `wpClosedID` (a previously-published, now-closed row with `published_at = 2026-06-15T12:00:00Z`, mirroring the existing write-path fixtures) → `UpdatePatch{Status:&valueobjects.Draft}` → re-read via `GetForUpdate` → `status="draft"`, `published_at` still `2026-06-15T12:00:00Z` (S1/S3), `updated_at` advanced. `UpdatePatch{Status:&valueobjects.Published}` → `status="published"`, `published_at` still `2026-06-15T12:00:00Z` (S2 — the `COALESCE(published_at, now())` branch preserves it; D6 zero SQL change).

- Verify: `cd backend && make test-integration` (sources `.env`; Postgres up + migrated; skips when `DATABASE_URL` unset) → new tests pass against the D1 SQL. Rollback: revert the test hunk.

### 3.2 RED/GREEN — Atomic field mix + stored `search_vector` (S6, S7)
- [x] 3.2 RED/GREEN — Add `TestUpdate_ClosedToDraftWithTitleApplies` and `TestUpdate_ClosedToPublishedWithDescriptionRegeneratesSearchVector`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/infrastructure/postgres/jobRepository_write_integration_test.go` (MOD): closed row + `UpdatePatch{Status:&Draft, Title:&"New"}` → re-read shows `status="draft"` AND `title="New"` (one statement, S6); closed row + `UpdatePatch{Status:&Published, Description:&"new body"}` → re-read shows `status="published"`, `description="new body"`, and `jobs.search_vector @@ websearch_to_tsquery('spanish', '<new-description-token>')` is true (STORED column regenerated in the same UPDATE — S7).

- Verify: `cd backend && make test-integration` → new tests pass. Rollback: revert the test hunk.

### 3.3 RED/GREEN — Active-company gate outcomes (S10, S11, S12, S29)
- [x] 3.3 RED/GREEN — Add `TestUpdate_SuspendedCompanyReturnsErrCompanyNotActive`, `TestUpdate_PendingVerificationCompanyReturnsErrCompanyNotActive`, `TestUpdate_ActiveCompanyPassesGuard`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/infrastructure/postgres/jobRepository_write_integration_test.go` (MOD): in-transaction, set `seededCompanyIDs[0]` to `'suspended'` (and separately `'pending_verification'`) then `Update` on a row of that company → `entities.ErrCompanyNotActive` AND the row NOT updated (re-read confirms prior state — S10/S11); active company → `nil` (S12). Assert `GuardPassed=false, UpdatedCount=0` semantics through the adapter result (409 "company is not active" body is already pinned by the existing handler `classifyError` branch — no handler change, S29 regression).

- Verify: `cd backend && make test-integration` → new tests pass; pre-existing `TestUpdate_CASMismatchReturnsErrJobNotFound` and `TestUpdate_CrossCompanyUpdateAffectsZeroRows` stay green (guard vs CAS-miss distinction — D1 result matrix). Rollback: revert the test hunk.

### 3.4 RED/GREEN — Atomicity: no read-then-write TOCTOU (S13)
- [x] 3.4 RED/GREEN — Add `TestUpdate_GuardIsAtomicWithUpdate`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/infrastructure/postgres/jobRepository_write_integration_test.go` (MOD): `GetForUpdate` succeeds against an active company, then the SAME transaction suspends the company, then `Update` runs → `entities.ErrCompanyNotActive` (the UPDATE's `EXISTS (SELECT 1 FROM active)` wins — guard and write are one statement; S13). This pins the central D1 decision: the guard outcome is observable (`guard_passed=false`) rather than collapsing into `ErrJobNotFound`/`ErrConcurrencyConflict`.

- Verify: `cd backend && make test-integration` → new test passes; full write-path integration suite green. Commit **C** lands here. Rollback: revert Commit C.

---

## Phase 4 — Full-suite gate (apply-owned verification)

### 4.1 — Full-suite verification for the whole change
- [ ] 4.1 — Run the complete verification set. <!-- sdd-owner: implementation -->

- Verify: `cd backend && go test ./...` (unit — strict-TDD green, RED tests pre-date their GREEN code); `cd backend && go vet ./...` clean; `gofmt -l backend/` empty (or `gofmt -w` applied and re-verified); `cd backend && go build ./...`; `go tool sqlc generate` idempotent (second run → empty `git diff`); optionally `cd backend && make test-integration`. Confirm `jobs.sql.go`/`querier.go` are regenerated (not hand-edited), Commit A/B/C each carry tests with their behavior, and the D8 locked scope held (6 files changed, nothing else).

---

## Phase 5 — Optional cosmetic cleanup

### 5.1 REFACTOR (optional, tracked) — Stale sentinel doc comment (design §3 cosmetic note)
- [ ] 5.1 REFACTOR — Refresh the `entities.ErrInvalidStatusTransition` doc comment. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/domain/entities/job.go` (MOD — **comment-only, optional**): the sentinel's doc comment ("the closed-terminal rule… rejects ANY body") is stale under D5. If refreshed, it is a separate comment-only commit that does not alter the compiled surface; the D8 inventory lists this file as UNCHANGED, so skipping this task is fully compliant — do it only to keep the codebase's docs honest.

- Verify: `cd backend && go test ./...` unchanged green; `git diff --stat backend/internal/features/jobs/domain/entities/job.go` shows comment lines only.

---

## Post-apply (parent-owned)

- Start or reuse bounded review of the merged change: trace tasks to commits (A/B/C and optional D); confirm `[x]` only on landed commits; D1–D8 honored (especially D4 `default` kept, D1 `:one` guard shape, D7 regen of BOTH generated files); the `jobs.sql.go`/`querier.go` files are generated-not-hand-edited; the two stale closed-terminal tests were narrowed in Commit A; and the **size-exception record exists** if single-pr delivery was kept (or the chained-PR split was adopted after the forecast flag). <!-- sdd-owner: parent -->
- Lifecycle gate: close the change (or route to `openspec/changes/archive/`) only after Phase 4.1 is green and the review above is resolved. <!-- sdd-owner: parent -->

---

## Scenario coverage matrix (design §7 checklist → tasks; layer: U=unit, A=adapter, I=SQL integration, R=existing regression)

| # | Delta scenario | Task(s) | Layer |
|---|---|---|---|
| S1 | closed → draft re-opens to draft | 1.2 (`TestEditJob_ClosedToDraftReopens`) + 3.1 (`TestUpdate_ClosedToDraftReopens`) | U + I |
| S2 | closed → published preserves `published_at` | 1.2 + 3.1 (`TestUpdate_ClosedToPublishedPreservesPublishedAt`) | U + I |
| S3 | `published_at` preserved across cycles | 3.1 | I |
| S4 | closed → closed returns 400 | 1.1 (FullTable Closed→Closed false) + 1.3 (`TestEditJob_ClosedToClosedRejects`) | U |
| S5 | re-open through the same gate as PATCH/CREATE | 1.2 (existing middleware gate untouched — regression R) | U/R |
| S6 | closed → draft + title atomic | 1.4 + 3.2 (`TestUpdate_ClosedToDraftWithTitleApplies`) | U + I |
| S7 | closed → published + description regenerates `search_vector` | 1.4 + 3.2 (`TestUpdate_ClosedToPublishedWithDescriptionRegeneratesSearchVector`) | U + I |
| S8 | field-only PATCH on closed → 400 | 1.3 (`TestEditJob_ClosedFieldOnlyRejects`) + 1.1 (narrowed `TestEditJob_ClosedTerminalAnyBodyRejects`) | U |
| S9 | status absent leaves status unchanged | 1.3 (`TestEditJob_ClosedStatusAbsentLeavesClosed`) | U |
| S10 | suspended company → 409 | 3.3 (`TestUpdate_SuspendedCompanyReturnsErrCompanyNotActive`) | I |
| S11 | pending_verification company → 409 | 3.3 (`TestUpdate_PendingVerificationCompanyReturnsErrCompanyNotActive`) | I |
| S12 | active company passes the gate | 3.3 (`TestUpdate_ActiveCompanyPassesGuard` + every passing `TestUpdate_*`) | I |
| S13 | active check atomic with the UPDATE | 3.4 (`TestUpdate_GuardIsAtomicWithUpdate`) | I |
| S14 | stale CAS → 409 + latest editor view | 1.5 (`TestEditJob_ClosedReopenStaleCASReturnsConflict`) + existing `TestUpdate_CASMismatchReturnsErrJobNotFound` (R) | U + I(R) |
| S15 | cross-company re-open → 404 | existing `TestGetForUpdate_CrossCompanyReturnsErrJobNotFound` (R — unchanged) | I(R) |
| S16 | soft-deleted closed re-open → 404 | existing `TestGetForUpdate_SoftDeletedReturnsErrJobNotFound` (R) | I(R) |
| S17 | draft → published sets `published_at` now | existing `TestUpdate_DraftToPublishedSetsPublishedAt` (R) | I(R) |
| S18 | published → closed preserves `published_at` | existing `TestUpdate_PublishedToClosedPreservesPublishedAt` (R) | I(R) |
| S19 | status + field mix atomic | 1.4 (re-open variant) + existing `TestEditJob_StatusOnlyPatchReturns200` / `TestUpdate_PartialPatchLeavesAbsentFieldsIntact` (R) | U/I(R) |
| S20 | published field-only preserves `published_at` | existing `TestUpdate_PublishedToPublishedPreservesPublishedAt` (R) | I(R) |
| S21 | published → draft rejected | 1.1 (FullTable) + existing `TestEditJob_PublishedToDraftRejected` (R) | U(R) |
| S22 | draft → closed rejected | existing `TestEditJob_DraftToClosedRejected` (R) | U(R) |
| S23 | status-only PATCH allowed | existing `TestEditJob_StatusOnlyPatchReturns200` (R) | U(R) |
| S24 | closed → draft re-open allowed | 1.1 (FullTable Closed→Draft true) + 1.2 | U |
| S25 | closed → published re-open allowed | 1.1 (FullTable Closed→Published true) + 1.2 | U |
| S26 | invalid job id → 400 | existing `updateJobHandler_test.go` id-parse test (R) | R |
| S27 | 404 identical cross-company / non-existent | existing handler + `GetForUpdate` tests (R) | R |
| S28 | 409 carries latest editor view | 1.5 + existing `TestEditJob_CASMismatchReturnsConflict` / handler CAS test (R) | U/R |
| S29 | 409 non-active company carries "company is not active" | 1.5 (`TestEditJob_UpdateErrCompanyNotActivePropagates`) + 2.1/2.3 (adapter) + 3.3 (SQL) + existing handler `classifyError` branch (R — pinned by the create path) | U/A/I/R |
