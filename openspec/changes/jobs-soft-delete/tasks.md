# Tasks: `jobs-soft-delete`

Status: tasks (SDD phase). Grounded by `proposal.md`, the delta spec `specs/jobs/spec.md`, and `design.md`. Design decisions **D1–D9 are pinned and MUST NOT be re-opened**; every task references the decision IDs it implements so apply can trace. Scenario coverage maps every delta scenario (S1–S31, design §8 checklist) to at least one task — see the matrix at the bottom.

Apply notes (non-negotiable):
- Strict TDD (`strict_tdd: true`): RED tasks precede their GREEN counterpart. A RED task is satisfied when the focused test fails for the intended reason (missing method/type, wrong outcome, or a compile break on the port/sqlc shape are all accepted REDs — design §7 intro and `jobs-create` D10 precedent).
- Checkboxes are marked `[x]` **only after the corresponding commit lands**, never at drafting time.
- `backend/internal/db/jobs.sql.go` and `backend/internal/db/querier.go` are **generated by sqlc — never hand-edited**. Regen via `cd backend && go tool sqlc generate` (Makefile alias `make sqlc`).
- **D6 CRITICAL (atomic port-extension unit)**: extending `repositories.JobRepository` with `SoftDelete` breaks FIVE stub types + the postgres adapter's `var _` assertion in lockstep. The port extension + all five stub repairs + the adapter's real `SoftDelete` land in **one commit** (Commit A = the contiguous Phase 1.1 → 2.3 sequence; the tree does not fully compile between 1.2 and 2.3 — the accepted `jobs-create` D10 pattern; no full-suite gate before 2.3).
- **Commit grouping (work-unit commits — tests land with the behavior they verify):**
  - **Commit A** — `feat(jobs): soft-delete port contract, guard SQL, adapter, and use case` → tasks 1.1–2.4 (use case + port + 5 stubs + `SoftDeleteJob :one` + regen + adapter + adapter unit tests; D1/D2/D3/D4/D5/D6/D7). One atomic unit: the tree compiles and the unit suite is green only after 2.3 lands. `createJob_test.go` and `createJobHandler_test.go` need **no edit** (they use the shared `writeStubRepo` / `writeStubHandlerRepo` types — design §6 correction).
  - **Commit B** — `test(jobs): integration coverage for soft-delete guard and outcomes` → task 3.1 (`jobRepository_softDelete_integration_test.go`; D1/D2).
  - **Commit C** — `feat(jobs): add DELETE /jobs/{id} soft-delete handler` → tasks 4.1–4.2 (`softDeleteJobHandler_test.go` + `jobHandler.go`; D8).
  - **Commit D** — `feat(jobs): mount gated DELETE /jobs/{id} at the composition root` → tasks 5.1–5.2 (`main_test.go` + `main.go`; D8).
- Locked scope (D9): exactly **17 authored files** change (6 production + 1 port + 4 new test files + 6 MOD test files) + 2 generated files. DTOs, read side, migrations, `domain/entities`, `JobForUpdate`, `UpdateJob`, `classifyError`, and `parseIfUnmodifiedSince` are untouched. Rollback = revert the merge commit; no migration, no schema change, no new package.

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~1,120–1,560 authored additions+deletions across 17 authored files (excl. ~70–100 regenerated sqlc in `internal/db/jobs.sql.go` + ~2–4 in `querier.go`, which are excluded from the authored count but included in snapshot identity) |
| 400-line budget risk | High |
| Chained PRs recommended | Yes |
| Suggested split | PR 1 (Phases 1–2: use case + port + SQL + adapter — Commit A) → PR 2 (Phase 3: SQL integration — Commit B) → PR 3 (Phase 4: handler — Commit C) → PR 4 (Phase 5: composition root — Commit D) |
| Delivery strategy | single-pr (pinned by preflight — the size-exception already accepted for `jobs-reopen` applies to this change too; do NOT auto-chain, but report the forecast honestly) |
| Chain strategy | size-exception |

```text
Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: size-exception
400-line budget risk: High
```

Rationale (per-file authored estimate, using design §3/§5 exact shapes): `db/queries/jobs.sql` ~55–75 (D1 query block + doc comment); `application/usecases/softDeleteJob.go` (NEW) ~45–60 (D5 flow + package doc); `application/usecases/jobService.go` ~15–25 (seam + interface + `var _`); `domain/repositories/jobRepository.go` ~10–15 (port method + doc contract); `infrastructure/postgres/jobRepository.go` ~45–60 (`SoftDelete` + `buildSoftDeleteJobParams` + `mapSoftDeleteError`); `infrastructure/http/jobHandler.go` ~40–55 (handler + accessor field); `cmd/api/main.go` ~1–2 (route line); `application/usecases/softDeleteJob_test.go` (NEW) ~190–260 (7 tests incl. table-driven status-agnostic); `infrastructure/postgres/jobRepository_softDelete_test.go` (NEW) ~55–85 (2 unit tests); `infrastructure/postgres/jobRepository_softDelete_integration_test.go` (NEW) ~330–430 (13 integration tests, fixtures reused); `infrastructure/http/softDeleteJobHandler_test.go` (NEW) ~230–310 (10 handler/route-boundary tests); `cmd/api/main_test.go` ~55–85 (AST guard + helper); 5 stub-repair files ~50–95 (`writeStubRepo`/`writeStubHandlerRepo` programmable ~20–35 each, three default-`nil` ~3–8 each). Sum ≈ 1,120–1,560 — **2.8–3.9× the 400-line budget**. Forecast rules require recommending chained PRs at High risk; because the preflight pinned `single-pr` and the size-exception is **already accepted** (same decision as `jobs-reopen`), no new decision is needed before apply — the apply agent proceeds single-pr, recording the size-exception record with the merge, and does NOT auto-chain. Tasks are already split into autonomous work units with start/finish/verify/rollback boundaries so either path proceeds.

---

## Phase 1 — Use case + port contract (design Phase A; D4/D5/D6)

Files: `backend/internal/features/jobs/application/usecases/softDeleteJob_test.go` (NEW), `backend/internal/features/jobs/application/usecases/softDeleteJob.go` (NEW), `backend/internal/features/jobs/application/usecases/jobService.go` (MOD), `backend/internal/features/jobs/domain/repositories/jobRepository.go` (MOD), `backend/internal/features/jobs/application/usecases/updateJob_test.go` (MOD — `writeStubRepo` repair).
Commit group: **Commit A** (contiguous with Phase 2 — the atomic port-extension unit).

### 1.1 RED — Use-case behavior tests (design §7 items 1–7; D4/D5/D2)
- [x] 1.1 RED — Author `softDeleteJob_test.go` with the seven use-case tests. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/application/usecases/softDeleteJob_test.go` (NEW), driving `writeStubRepo`:
- `TestSoftDeleteJob_SuccessCallsSoftDeleteWithCAS` — `GetForUpdate` returns a row; assert `repo.SoftDelete` called with `(id, companyID, current.UpdatedAt)` and use case returns `(nil, nil)`.
- `TestSoftDeleteJob_StatusAgnostic` (table over draft/published/closed) — the use case never branches on `current.JobStatus`; each status flows `GetForUpdate → SoftDelete` and returns `(nil, nil)`.
- `TestSoftDeleteJob_CASMismatchReturnsConflictWithView` — stale `ifUnmodifiedSince` → `(toEditorView(current), ErrConcurrencyConflict)`, `SoftDelete` NOT called.
- `TestSoftDeleteJob_ZeroTokenReturnsConflict` — `time.Time{}` token → mismatch → `ErrConcurrencyConflict` (covers missing/malformed header at the use-case layer).
- `TestSoftDeleteJob_GetForUpdateNotFoundPropagates` — `GetForUpdate` → `ErrJobNotFound` → `(nil, ErrJobNotFound)`, `SoftDelete` NOT called.
- `TestSoftDeleteJob_SoftDeleteErrCompanyNotActivePropagates` — stub `softDeleteErr = ErrCompanyNotActive` → `(nil, ErrCompanyNotActive)`, no re-read (`getForUpdateCalls == 1`).
- `TestSoftDeleteJob_SoftDeleteErrJobNotFoundPropagatesNoReread` — stub `softDeleteErr = ErrJobNotFound` → `(nil, ErrJobNotFound)`, `getForUpdateCalls == 1` (pins D2's no-re-read).

- Verify: `cd backend && go test ./internal/features/jobs/application/usecases/ -run SoftDeleteJob` → **RED** (compile: `JobService.SoftDeleteJob`, port `SoftDelete`, and `writeStubRepo.SoftDelete` do not exist yet — the accepted port-extension compile RED, design §7 intro). Rollback: revert the test hunk.

### 1.2 GREEN — Use case, service seam, port extension, `writeStubRepo` repair (D4/D5/D6)
- [x] 1.2 GREEN — Author `softDeleteJob.go`, the `jobService.go` seam, the port `SoftDelete`, and the `writeStubRepo` repair. <!-- sdd-owner: implementation -->

- `backend/internal/features/jobs/application/usecases/softDeleteJob.go` (NEW): `func (s *JobService) SoftDeleteJob(ctx context.Context, companyID, jobID uuid.UUID, ifUnmodifiedSince time.Time) (*dtos.JobEditorViewDto, error)` per D5 — (1) `GetForUpdate` (0 rows collapse to `ErrJobNotFound` → 404); (2) CAS compare `!ifUnmodifiedSince.Equal(current.UpdatedAt)` → `(toEditorView(current), entities.ErrConcurrencyConflict)`; (3) `s.repo.SoftDelete(ctx, jobID, companyID, current.UpdatedAt)` with `ErrCompanyNotActive`/`ErrJobNotFound` propagating untouched; (4) **no re-read** → `(nil, nil)`. Package doc mirrors `updateJob.go`'s header. `toEditorView` is the package-private projection from `updateJob.go`, reused verbatim.
- `backend/internal/features/jobs/application/usecases/jobService.go` (MOD): add the method seam next to `EditJob` — `var _ SoftDeleteJobUseCase = (*JobService)(nil)` + the `SoftDeleteJobUseCase` interface (D4 tuple signature). `NewJobService` signature unchanged.
- `backend/internal/features/jobs/domain/repositories/jobRepository.go` (MOD): add `SoftDelete(ctx context.Context, id, companyID uuid.UUID, casUpdatedAt time.Time) error` with the D6 doc contract (`ErrCompanyNotActive` / `ErrJobNotFound` / `ErrInvalidStatusTransition` defense-in-depth / pass-through). `Search`, `GetByID`, `GetForUpdate`, `Update`, `Create` unchanged.
- `backend/internal/features/jobs/application/usecases/updateJob_test.go` (MOD): `writeStubRepo.SoftDelete` with programmable `softDeleteErr` + capture `softDeleteCalls`, `lastSoftDeleteID`, `lastSoftDeleteCompany`, `lastSoftDeleteCas`.

- Verify: tasks 1.1 tests pass; `cd backend && go test ./internal/features/jobs/application/...` compiles and is green. Note: the `postgres` package still fails its `var _` assertion until 2.3 — the contiguous sequence continues; **no full-suite gate before 2.3**. Rollback: revert 1.1+1.2 together (port + `writeStubRepo` are part of the atomic unit).

---

## Phase 2 — SQL + sqlc + adapter (design Phase B; D1/D2/D3/D7)

Files: `backend/db/queries/jobs.sql` (MOD), `backend/internal/db/jobs.sql.go` + `backend/internal/db/querier.go` (generated regen), `backend/internal/features/jobs/infrastructure/postgres/jobRepository_softDelete_test.go` (NEW), `backend/internal/features/jobs/infrastructure/postgres/jobRepository.go` (MOD), stub repairs in `backend/internal/features/jobs/domain/repositories/jobRepository_test.go`, `backend/internal/features/jobs/application/usecases/searchJobs_test.go`, `backend/internal/features/jobs/infrastructure/http/handler_test.go`, `backend/internal/features/jobs/infrastructure/http/updateJobHandler_test.go` (all MOD).
Commit group: **Commit A** (contiguous with Phase 1 — 2.1/2.2/2.3 MUST land in the same commit as 1.1/1.2; the package is compile-broken between 1.2 and 2.3).

### 2.1 RED (compile) — Author `SoftDeleteJob :one` and regenerate (D1, D7)
- [x] 2.1 RED — Author the D1 CTE guard; run sqlc; confirm the generated seam. <!-- sdd-owner: implementation -->

`backend/db/queries/jobs.sql` (MOD): append the `SoftDeleteJob :one` query exactly per design §3 D1 — (a) doc comment block (atomic soft-delete + CAS + active-company guard; minimal SET list rationale); (b) `active` CTE (`SELECT id FROM companies WHERE id = sqlc.arg('company_id')::uuid AND status = 'active'`); (c) `upd` CTE wrapping `UPDATE jobs SET deleted_at = now(), updated_at = now()` with `RETURNING id`; (d) WHERE `id / company_id / deleted_at IS NULL / updated_at = sqlc.arg('cas_token')::timestamptz / EXISTS (SELECT 1 FROM active)` — row-narrowing predicates identical to `UpdateJob`; (e) scalar `SELECT EXISTS(SELECT 1 FROM active) AS guard_passed, (SELECT count(*) FROM upd) AS deleted_count;` (always exactly one row). `SearchJobs`, `GetJobByID`, `GetJobForUpdate`, `UpdateJob`, `CreateJob` stay byte-for-byte unchanged.

Then run `cd backend && go tool sqlc generate`: `backend/internal/db/jobs.sql.go` gains `SoftDeleteJobParams {CompanyID, ID, CasToken pgtype.Timestamptz}` (arg order pinned by first textual appearance: `company_id` in the CTE, then `id`, then `cas_token`), `SoftDeleteJobRow {GuardPassed bool; DeletedCount int64}` (names pinned by the SQL aliases — a naming drift fails the adapter compile), and `SoftDeleteJob(ctx, arg) (SoftDeleteJobRow, error)`; `backend/internal/db/querier.go`'s `Querier` gains the entry. `UpdateJobParams`/`UpdateJobRow` are unchanged.

- Verify: `cd backend && go tool sqlc generate` succeeds; `cd backend && go build ./...` → **RED (compile)** — the adapter and the four unrepaired stubs lack `SoftDelete` (design §7 item 8). Do NOT fix them yet — 2.3 is the same commit. Rollback: revert the `jobs.sql` hunk + regen together.

### 2.2 RED — Adapter helper unit tests (D2/D3)
- [x] 2.2 RED — Author `jobRepository_softDelete_test.go`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/infrastructure/postgres/jobRepository_softDelete_test.go` (NEW; named after the source file per repo convention, mirroring `updateJobRepository_test.go`), all DB-free:
- `TestMapSoftDeleteError` (table-driven): `nil` → `nil`; bare `pgx.ErrNoRows` → `entities.ErrCompanyNotActive`; wrapped `fmt.Errorf("...: %w", pgx.ErrNoRows)` → `entities.ErrCompanyNotActive` (errors.Is path — the `ErrNoRows` check runs BEFORE `errors.As`); `*pgconn.PgError{Code:"23514"}` direct + wrapped → `entities.ErrInvalidStatusTransition`; unknown `PgError` → pass-through; non-pg error → pass-through. Assert there is **NO `23503` branch** (D3 — soft-delete never touches `company_id`).
- `TestBuildSoftDeleteJobParams`: `(id, companyID, casUpdatedAt)` → `SoftDeleteJobParams{CompanyID, ID, CasToken}` with `CasToken.Time == casUpdatedAt` and `CasToken.Valid == true`.

- Verify: `cd backend && go test ./internal/features/jobs/infrastructure/postgres/ -run 'MapSoftDeleteError|BuildSoftDeleteJobParams'` → **RED** (compile: helpers missing). Rollback: revert the test hunk.

### 2.3 GREEN — Adapter `SoftDelete` + remaining stub repair (D1/D2/D3/D6)
- [x] 2.3 GREEN — Implement `SoftDelete`, `buildSoftDeleteJobParams`, `mapSoftDeleteError`; repair the four remaining stubs. <!-- sdd-owner: implementation -->

- `backend/internal/features/jobs/infrastructure/postgres/jobRepository.go` (MOD): per design §5.5 — `SoftDelete` calls `r.queries.SoftDeleteJob(ctx, buildSoftDeleteJobParams(id, companyID, casUpdatedAt))`; `err != nil` → `mapSoftDeleteError(err)`; `!row.GuardPassed` → `entities.ErrCompanyNotActive`; `row.DeletedCount == 0` → `entities.ErrJobNotFound`; else `nil`. Add `buildSoftDeleteJobParams` (D1 param order) and `mapSoftDeleteError` exactly per D3 (`pgx.ErrNoRows` BEFORE `errors.As`; `23514 → ErrInvalidStatusTransition` defense-in-depth; **no `23503`**; pass-through otherwise). `var _ repositories.JobRepository = (*JobRepository)(nil)` stays.
- Stub repairs — **atomic with the port extension (D6)**: `stubJobRepo` (`domain/repositories/jobRepository_test.go`), `stubJobRepository` (`application/usecases/searchJobs_test.go`), `stubRepo` (`infrastructure/http/handler_test.go`) gain a default `SoftDelete` returning `nil`; `writeStubHandlerRepo` (`infrastructure/http/updateJobHandler_test.go`) gains the programmable surface (`softDeleteErr` + capture `softDeleteCalls`/`lastSoftDeleteID`/`lastSoftDeleteCompany`/`lastSoftDeleteCas`), ready for Phase 4.

- Verify: 2.2 tests pass; `cd backend && go test ./internal/features/jobs/...` green end-to-end (tree compiles again); `cd backend && go build ./...`. Commit **A** lands here (1.1–2.3 atomically). Rollback: revert Commit A — `jobs.sql` + `jobRepository.go` (port + adapter) + the two generated files + the five stub test files revert in lockstep.

### 2.4 REFACTOR (optional, tracked) — Adapter hygiene
- [x] 2.4 REFACTOR — Confirm generated-not-hand-edited and no duplicated helpers. <!-- sdd-owner: implementation -->

`git diff --stat backend/internal/db/` to confirm `jobs.sql.go`/`querier.go` changes are exactly the regen; confirm `mapSoftDeleteError` mirrors `mapUpdateError`'s ordering without duplicating `mapCreateError`'s `23503` branch; `SoftDelete`'s row-inspection has no dead branches per the D2 result matrix.

- Verify: `cd backend && go test ./internal/features/jobs/...` still green; `go tool sqlc generate` re-run is idempotent (`git diff` empty after a second run).

---

## Phase 3 — SQL-level integration (design Phase C; D1/D2)

File: `backend/internal/features/jobs/infrastructure/postgres/jobRepository_softDelete_integration_test.go` (NEW, `//go:build integration`).
Commit group: **Commit B** (task 3.1 lands alone; RED and GREEN coincide — deferred RED for the D1 SQL, exactly as `jobs-create` Phase 7 did).

### 3.1 RED/GREEN — Guard outcomes, CAS, immutability, atomicity at the SQL level (S16–S27, S30, S31)
- [ ] 3.1 RED/GREEN — Add the 13-test integration suite. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/infrastructure/postgres/jobRepository_softDelete_integration_test.go` (NEW, `//go:build integration`, transaction-rollback isolation, skip when `DATABASE_URL` unset — reuses package-level fixtures `wpDraftID`/`wpPublishedID`/`wpClosedID`/`wpDeletedID`/`wpCrossCoID`, `setupWritePath`, `seededCompanyIDs`; mirror `jobRepository_create_integration_test.go`'s new-file precedent). Tests (design §7 items 11–23):
- `TestSoftDelete_DraftRowDeletes` — `SoftDelete(wpDraftID)` → `nil`; raw SQL asserts `deleted_at IS NOT NULL`; `repo.GetByID` → `ErrJobNotFound` (read invisibility).
- `TestSoftDelete_PublishedRowDeletes` / `TestSoftDelete_ClosedRowDeletes` — `nil`; `published_at` preserved; `deleted_at` set.
- `TestSoftDelete_SetsDeletedAtAndAdvancesUpdatedAt` — raw SQL asserts `deleted_at IS NOT NULL` and `updated_at > before.UpdatedAt` (exactly once).
- `TestSoftDelete_PreservesImmutables` — maximal snapshot before; after SoftDelete, `title`/`description`/`status`/`work_mode`/`employment_type`/`seniority`/`location`/`salary_min`/`salary_max`/`salary_currency`/`published_at`/`id`/`company_id`/`created_at` unchanged; only `deleted_at` + `updated_at` moved.
- `TestSoftDelete_SearchVectorUnchangedAndExcludedFromListing` — raw SQL asserts `search_vector` still matches the old `title`+`description` tokens; `repo.Search` (active company, published predicate) excludes the row.
- `TestSoftDelete_SecondDeleteReturnsErrJobNotFound` — `SoftDelete(wpDeletedID)` → `ErrJobNotFound`; also `SoftDelete` a fresh row twice → second is `ErrJobNotFound`.
- `TestSoftDelete_CrossCompanyReturnsErrJobNotFound` — `SoftDelete(wpCrossCoID, seededCompanyIDs[0])` → `ErrJobNotFound`.
- `TestSoftDelete_CASMismatchReturnsErrJobNotFound` — stale `casUpdatedAt` → `ErrJobNotFound`, row NOT deleted (adapter-level; the use case catches the common case earlier as 409).
- `TestSoftDelete_SuspendedCompanyReturnsErrCompanyNotActive` / `TestSoftDelete_PendingVerificationCompanyReturnsErrCompanyNotActive` — in-transaction company status change → `ErrCompanyNotActive`, row NOT deleted.
- `TestSoftDelete_ActiveCompanyPassesGuard` — active company → `nil`.
- `TestSoftDelete_GuardIsAtomicWithUpdate` — `GetForUpdate` while active, then suspend in-transaction, then `SoftDelete` → `ErrCompanyNotActive` (the UPDATE's `EXISTS` wins; no TOCTOU).

- Verify: `cd backend && make test-integration` (sources `.env`; Postgres up + migrated; skips when `DATABASE_URL` unset) → new tests pass against the D1 SQL; existing `TestUpdate_*` write-path integration tests stay green (guard vs CAS-miss distinction — D2 matrix). Commit **B** lands here. Rollback: revert Commit B.

---

## Phase 4 — HTTP handler (design Phase D; D8)

Files: `backend/internal/features/jobs/infrastructure/http/softDeleteJobHandler_test.go` (NEW), `backend/internal/features/jobs/infrastructure/http/jobHandler.go` (MOD).
Commit group: **Commit C** (tasks 4.1–4.2 land together).

### 4.1 RED — Handler + route-boundary tests (design §7 items 24–33; D8)
- [ ] 4.1 RED — Author `softDeleteJobHandler_test.go`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/infrastructure/http/softDeleteJobHandler_test.go` (NEW; mirror `newUpdateJobRouter` in `updateJobHandler_test.go` → a `newSoftDeleteJobRouter(repo, cc)` mounting `h.JobHandlers().SoftDeleteJob` at `r.Delete("/jobs/{id}", …)` with CompanyContext injection; drive `writeStubHandlerRepo` — its programmable `SoftDelete` surface landed in Commit A):
- `TestSoftDeleteJob_MissingCompanyContextReturns500` — no `CompanyContext` → 500, `SoftDelete` NOT called.
- `TestSoftDeleteJob_InvalidUUIDReturns400` — `/jobs/not-a-uuid` → 400.
- `TestSoftDeleteJob_StaleCASReturns409WithView` / `TestSoftDeleteJob_MissingCASReturns409WithView` / `TestSoftDeleteJob_MalformedCASReturns409WithView` — stale / absent / `not-a-timestamp` `If-Unmodified-Since` → 409 + editor view body (decode as `JobEditorViewDto`).
- `TestSoftDeleteJob_NotFoundReturns404` — `GetForUpdate` → `ErrJobNotFound` → 404 (cross-company/non-existent/soft-deleted indistinguishable).
- `TestSoftDeleteJob_CompanyNotActiveReturns409` — `SoftDelete` → `ErrCompanyNotActive` → 409 `{"error":"company is not active"}`.
- `TestSoftDeleteJob_SuccessReturns204EmptyBody` — success → 204, empty body.
- `TestSoftDeleteJob_MissingAuthReturns401` — mount `RequireAuth(denyAllVerifier)` ahead of the handler; no `Authorization` → 401.
- `TestSoftDeleteJob_DeleteNotServedByPublicMount` — DELETE through public `h.Routes()` under `/jobs` → chi 404 (not matched).

- Verify: `cd backend && go test ./internal/features/jobs/infrastructure/http/ -run SoftDeleteJob` → **RED** (compile: `softDeleteJob`/`JobHandlers.SoftDeleteJob` missing). Rollback: revert the test hunk.

### 4.2 GREEN — Implement the handler surface (D8)
- [ ] 4.2 GREEN — Implement `softDeleteJob` + `JobHandlers.SoftDeleteJob`. <!-- sdd-owner: implementation -->

`backend/internal/features/jobs/infrastructure/http/jobHandler.go` (MOD): `softDeleteJob` per design §3 D8 — `requireCompanyContext` (fail-closed 500), `uuid.Parse(chi.URLParam(r, "id"))` → 400 `"invalid job id"`, `parseIfUnmodifiedSince(r.Header.Get("If-Unmodified-Since"))` (reused verbatim), `h.service.SoftDeleteJob(...)`; the `errors.Is(err, entities.ErrConcurrencyConflict)` special-case writes the returned view as 409 (same branch `updateJob` has); otherwise `classifyAndWriteError` (unchanged); success → `w.WriteHeader(http.StatusNoContent)`. `JobHandlers` gains `SoftDeleteJob http.HandlerFunc`; the accessor adds `SoftDeleteJob: http.HandlerFunc(h.softDeleteJob)`. `classifyError` / `requireCompanyContext` / `parseIfUnmodifiedSince` unchanged.

- Verify: 4.1 tests pass; `cd backend && go test ./internal/features/jobs/...`. Commit **C** lands here. Rollback: revert 4.1+4.2 together.

### 4.3 REFACTOR (optional, tracked) — Handler hygiene
- [ ] 4.3 REFACTOR — Confirm no parallel projections or duplicated classification. <!-- sdd-owner: implementation -->

Confirm the 409-with-view body is the DTO returned by the use case (no handler-side re-read/projection); `classifyError` gained no new branches; `parseIfUnmodifiedSince` is the single RFC 3339 parser.

- Verify: `cd backend && go test ./internal/features/jobs/...` still green; `cd backend && go vet ./internal/features/jobs/...` clean.

---

## Phase 5 — Composition root + AST guard (design Phase E; D8)

Files: `backend/cmd/api/main_test.go` (MOD), `backend/cmd/api/main.go` (MOD).
Commit group: **Commit D** (tasks 5.1–5.2 land together).

### 5.1 RED — Add the gated-DELETE AST guard (design §7 item 34)
- [ ] 5.1 RED — Add `TestJobsSoftDeleteRoute_MountedBehindGates` (and the `deletePathLiteral` test helper it needs). <!-- sdd-owner: implementation -->

`backend/cmd/api/main_test.go` (MOD): add `TestJobsSoftDeleteRoute_MountedBehindGates` mirroring `TestJobsWriteRoute_MountedBehindGates`/`TestJobsCreateRoute_MountedBehindGates` — an AST walk finds a chi `Delete("/jobs/{id}", …)` mutation whose inner `With(...)` argument list references BOTH `requireAuth` and `requireRecruiter` (reuse `isWithCall`/`referencesIdentifier`; add a `deletePathLiteral` helper alongside the existing `patchPathLiteral`/`postPathLiteral` — test-side tooling, lands with the RED hunk so the RED is an assertion failure, not a compile error). Before the route line exists, the walk finds zero gated `Delete("/jobs/{id}")` mutations.

- Verify: `cd backend/cmd/api && go test .` → **RED** (the new guard fails: no gated DELETE route in `main.go` yet; existing guards stay green). Rollback: revert the test hunk.

### 5.2 GREEN — Add the gated DELETE route (D8)
- [ ] 5.2 GREEN — Add `r.With(requireAuth, requireRecruiter).Delete("/jobs/{id}", jobHandlers.SoftDeleteJob)`. <!-- sdd-owner: implementation -->

`backend/cmd/api/main.go` (MOD): one line below the existing PATCH/POST on the gated subtree — `r.With(requireAuth, requireRecruiter).Delete("/jobs/{id}", jobHandlers.SoftDeleteJob)`. `requireAuth` and `requireRecruiter` are already hoisted at `run()` scope. The public `r.Mount("/jobs", jobHandler.Routes())` MUST NOT gain the DELETE (same routing-split defense as PATCH/POST — a DELETE to the public mount is not matched → chi 404).

- Verify: `cd backend/cmd/api && go test .` green (the new AST guard + the two existing guards + `TestJobsMount_PublicReadRoutes`); `cd backend && go build ./...`; `cd backend && go vet ./...`. Commit **D** lands here. Rollback: revert 5.1+5.2; reverting `main.go` alone removes the route while public GETs and gated PATCH/POST keep working.

---

## Phase 6 — Full-suite gate (apply-owned verification)

### 6.1 — Full-suite verification for the whole change
- [ ] 6.1 — Run the complete verification set. <!-- sdd-owner: implementation -->

- Verify: `cd backend && go test ./...` (unit — strict-TDD green, RED tests pre-date their GREEN code); `cd backend && go vet ./...` clean; `gofmt -l backend/` empty; `cd backend && go build ./...`; `go tool sqlc generate` idempotent (second run → empty `git diff`); optionally `cd backend && make test-integration`. Confirm `jobs.sql.go`/`querier.go` are regenerated (not hand-edited), Commits A–D each carry tests with their behavior, Commit A holds the atomic port-extension unit (port + 5 stubs + adapter + SQL + regen), and the D9 locked scope held (17 authored files + 2 generated, nothing else).

---

## Post-apply (parent-owned)

- Start or reuse bounded review of the merged change: trace tasks to commits A–D; confirm `[x]` only on landed commits; D1–D9 honored (D1 `:one` guard shape with `{guard_passed, deleted_count}`; D2 no-re-read 404 mapping; D3 `pgx.ErrNoRows` before `errors.As` and no `23503`; D4 tuple return; D5 4-step flow; D6 atomic port-extension unit in Commit A; D7 regen of BOTH generated files; D8 handler + gated route line + public mount untouched; D9 the five-stub inventory incl. `stubJobRepo` and `stubRepo`); the `jobs.sql.go`/`querier.go` files are generated-not-hand-edited; and the **size-exception record exists** for the single-pr delivery (accepted decision from the `jobs-reopen` precedent). <!-- sdd-owner: parent -->
- Lifecycle gate: close the change (or route to `openspec/changes/archive/`) only after Phase 6.1 is green and the review above is resolved. <!-- sdd-owner: parent -->

---

## Scenario coverage matrix (design §8 checklist → tasks; layer: U=use-case unit, H=handler, I=SQL integration, A=adapter unit, AST=composition-root guard, R=existing regression)

| # | Delta scenario | Task(s) | Layer |
|---|---|---|---|
| S1 | recruiter can soft-delete a vacancy of their company | 1.1 (`TestSoftDeleteJob_SuccessCallsSoftDeleteWithCAS`) + 3.1 (`TestSoftDelete_DraftRowDeletes`) | U + I |
| S2 | owner passes the recruiter gate | 5.1/5.2 (route uses `requireRecruiter`) + existing `RequireCompanyRole` ordinal tests (R) | AST + R |
| S3 | no Authorization header → 401 | 4.1 (`TestSoftDeleteJob_MissingAuthReturns401`) | H |
| S4 | authenticated non-member → 403 | existing `RequireCompanyRole` tests (R — middleware short-circuit) | R |
| S5 | role too low → 403 | existing `RequireCompanyRole` tests (R) | R |
| S6 | invalid job id → 400 | 4.1 (`TestSoftDeleteJob_InvalidUUIDReturns400`) | H |
| S7 | 204 No Content with empty body on success | 4.1 (`TestSoftDeleteJob_SuccessReturns204EmptyBody`) + 1.1 (`TestSoftDeleteJob_SuccessCallsSoftDeleteWithCAS`) | H + U |
| S8 | missing CompanyContext fails closed → 500 | 4.1 (`TestSoftDeleteJob_MissingCompanyContextReturns500`) | H |
| S9 | DELETE not reachable through the public mount | 4.1 (`TestSoftDeleteJob_DeleteNotServedByPublicMount`) + existing `TestJobsMount_PublicReadRoutes` (R) | H + R |
| S10 | matching `updated_at` allows the soft-delete | 1.1 (`TestSoftDeleteJob_SuccessCallsSoftDeleteWithCAS`) + 3.1 (`TestSoftDelete_DraftRowDeletes`) | U + I |
| S11 | stale `updated_at` → 409 with latest editor view | 1.1 (`TestSoftDeleteJob_CASMismatchReturnsConflictWithView`) + 4.1 (`TestSoftDeleteJob_StaleCASReturns409WithView`) | U + H |
| S12 | missing `If-Unmodified-Since` → 409 with editor view | 1.1 (`TestSoftDeleteJob_ZeroTokenReturnsConflict`) + 4.1 (`TestSoftDeleteJob_MissingCASReturns409WithView`) | U + H |
| S13 | malformed `If-Unmodified-Since` → 409 with editor view | 4.1 (`TestSoftDeleteJob_MalformedCASReturns409WithView` — parse → zero token) | H |
| S14 | CAS conflict independent of company visibility | 1.1 (`TestSoftDeleteJob_CASMismatchReturnsConflictWithView` — CAS compare runs before the cross-company filter) | U |
| S15 | two concurrent writers, exactly one wins | 1.1 (`TestSoftDeleteJob_CASMismatchReturnsConflictWithView`) + 3.1 (`TestSoftDelete_CASMismatchReturnsErrJobNotFound`) | U + I |
| S16 | soft-delete advances `updated_at` exactly once | 3.1 (`TestSoftDelete_SetsDeletedAtAndAdvancesUpdatedAt`) | I |
| S17 | suspended company DELETE → 409 company is not active | 3.1 (`TestSoftDelete_SuspendedCompanyReturnsErrCompanyNotActive`) + 4.1 (`TestSoftDeleteJob_CompanyNotActiveReturns409`) | I + H |
| S18 | pending_verification company DELETE → 409 | 3.1 (`TestSoftDelete_PendingVerificationCompanyReturnsErrCompanyNotActive`) | I |
| S19 | active company DELETE passes the gate | 3.1 (`TestSoftDelete_ActiveCompanyPassesGuard`) | I |
| S20 | active check is atomic with the UPDATE | 3.1 (`TestSoftDelete_GuardIsAtomicWithUpdate`) | I |
| S21 | draft row is soft-deletable in one step | 3.1 (`TestSoftDelete_DraftRowDeletes`) | I |
| S22 | published row is soft-deletable in one step | 3.1 (`TestSoftDelete_PublishedRowDeletes`) | I |
| S23 | closed row is soft-deletable in one step | 3.1 (`TestSoftDelete_ClosedRowDeletes`) | I |
| S24 | `deleted_at` is the audit timestamp; no other column touched | 3.1 (`TestSoftDelete_PreservesImmutables`) | I |
| S25 | `search_vector` and partial index naturally consistent | 3.1 (`TestSoftDelete_SearchVectorUnchangedAndExcludedFromListing`) | I |
| S26 | second DELETE on already-soft-deleted → 404 | 3.1 (`TestSoftDelete_SecondDeleteReturnsErrJobNotFound`) + 1.1 (`TestSoftDeleteJob_GetForUpdateNotFoundPropagates`) | I + U |
| S27 | cross-company DELETE → 404 | 3.1 (`TestSoftDelete_CrossCompanyReturnsErrJobNotFound`) + existing `TestGetForUpdate_CrossCompanyReturnsErrJobNotFound` (R) | I + R |
| S28 | non-existent id DELETE → 404 | 1.1 (`TestSoftDeleteJob_GetForUpdateNotFoundPropagates`) + existing `TestGetForUpdate_NonExistentReturnsErrJobNotFound` (R) | U + R |
| S29 | PATCH re-open on soft-deleted → 404 (cross-reference) | existing `TestGetForUpdate_SoftDeletedReturnsErrJobNotFound` (R — unchanged) | R |
| S30 | soft-deleted hidden from `GET /jobs/{id}` (cross-reference) | 3.1 (`TestSoftDelete_DraftRowDeletes` — re-read `GetByID` → `ErrJobNotFound`) + existing `GetByID` visibility test (R) | I + R |
| S31 | soft-deleted excluded from `GET /jobs` (cross-reference) | 3.1 (`TestSoftDelete_SearchVectorUnchangedAndExcludedFromListing`) + existing `Search` visibility test (R) | I + R |
